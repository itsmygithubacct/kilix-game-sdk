# kilix-game-sdk

The shared runtime, rendering, interface, world, story, asset, and authoring
libraries used by Kilix games.

This repository gives games one recursively pinned SDK revision while keeping
the component libraries independently testable.
